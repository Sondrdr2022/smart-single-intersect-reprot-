% =========================
% CHƯƠNG 11
% =========================
\chapter{Mô hình thí nghiệm}

\section{Môi trường mô phỏng}

Hệ thống được kiểm thử trên môi trường mô phỏng vi mô SUMO (Simulation of Urban Mobility) phiên bản 1.22.0. SUMO cho phép quan sát chi tiết trạng thái của phương tiện, làn đường, và logic đèn giao thông. Việc sử dụng SUMO giúp kiểm tra hiệu năng bộ điều khiển trong nhiều kịch bản giao thông đa dạng, với khả năng tùy biến topology, traffic demand và mô phỏng các sự kiện đặc biệt như xe ưu tiên hoặc tắc nghẽn cục bộ.

\subsection{Thiết lập topology và logic đèn bằng NETEDIT}

Quá trình tạo môi trường mô phỏng bắt đầu bằng công cụ NETEDIT của SUMO. NETEDIT hỗ trợ:
\begin{itemize}
    \item Vẽ tuyến đường, làn rẽ trái/phải, nút giao thông.
    \item Thiết lập logic đèn tín hiệu: khai báo các pha (xanh/vàng/đỏ), trạng thái và thời lượng.
    \item Kiểm tra trực quan xung đột rẽ trái, khả năng phục vụ các hướng.
    \item Xuất các file cấu hình: \texttt{.net.xml}, \texttt{.tlLogic.xml}, \texttt{.sumocfg}, \texttt{.rou.xml}.
\end{itemize}
Việc này giúp đảm bảo topology mô phỏng sát thực tế, logic đèn đáp ứng yêu cầu kiểm thử và giảm thiểu lỗi cấu hình thủ công.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{g7t.png}
    \caption{Thiết lập mô hình thí nghiệm và logic đèn bằng NETEDIT}
    \label{fig:netedit_setup}
\end{figure}

\subsection{Tích hợp các file cấu hình vào mô phỏng SUMO}

Sau khi thiết kế trên NETEDIT, các file cấu hình được xuất ra gồm:
\begin{itemize}
    \item \texttt{net.xml}: Topology mạng lưới (làn, nút, tuyến).
    \item \texttt{rou.xml}: Luồng xe, tỷ lệ xuất hiện các loại phương tiện.
    \item \texttt{tlLogic.xml} hoặc \texttt{add.xml}: Logic đèn tín hiệu, chuỗi pha.
    \item \texttt{sumocfg}: Liên kết các file trên thành mô hình tổng thể.
\end{itemize}
Các file này là đầu vào cho bộ điều khiển Python, đảm bảo kiểm thử bám sát kịch bản thực tế.

\subsection{Tích hợp TraCI và các thành phần mô phỏng}

Giao tiếp giữa Python và SUMO được thực hiện qua TraCI, cho phép bộ điều khiển truy vấn trạng thái real-time, gửi lệnh điều chỉnh pha đèn và thu thập dữ liệu phục vụ đào tạo RL agent. Thành phần mô phỏng gồm:

\begin{itemize}
    \item \textbf{Network topology}: Mô hình nút giao, tuyến đường, làn rẽ trái/phải, điểm vào/ra.
    \item \textbf{Traffic demand}: Cấu hình luồng xe, tần suất xuất hiện, loại phương tiện.
    \item \textbf{Logic đèn tín hiệu}: Chuỗi pha, trạng thái xanh/vàng/đỏ, thời lượng.
    \item \textbf{Event injection}: Kịch bản xuất hiện xe ưu tiên, sự kiện tắc nghẽn, thay đổi topology.
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Untitled diagram _ Mermaid Chart-2025-09-03-023025.png}
    \caption{Set up mô phỏng SUMO với tích hợp TraCI, event injection}
    \label{fig:sumo_setup_traci}
\end{figure}

\section{Cấu hình cơ bản}

Cấu hình thí nghiệm nhằm kiểm chứng hiệu quả, độ linh hoạt và khả năng phục hồi của bộ điều khiển thông minh trong các kịch bản thực tế.

\subsection{Cấu hình mạng lưới giao thông}

\begin{itemize}
    \item \textbf{Nút giao cơ bản}: 4 hướng, mỗi hướng gồm làn đi thẳng, rẽ trái, rẽ phải.
    \item \textbf{Thiết lập logic đèn}: 8–12 pha, đảm bảo đủ cho các hướng, có pha bảo vệ rẽ trái, chèn pha vàng.
    \item \textbf{Traffic demand}: Dòng xe ngẫu nhiên, các burst lưu lượng giờ cao điểm, xuất hiện xe ưu tiên định kỳ.
    \item \textbf{Tham số mô phỏng}:
        \begin{itemize}
            \item Thời gian mô phỏng: 1000–5000 bước.
            \item Tỷ lệ xe ưu tiên: 2–5\%.
            \item Cấu hình min\_green, max\_green, cycle\_length phù hợp thực tế.
        \end{itemize}
\end{itemize}

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{ưD.png}
        \caption*{(a) Topology nút giao 4 hướng và traffic demand}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{DƯd.png}
        \caption*{(b) Cấu hình logic đèn tín hiệu trên giao diện NETEDIT}
    \end{minipage}
    \caption{Ảnh set up topology mạng lưới, traffic demand, logic đèn}
    \label{fig:sumo_network_config}
\end{figure}

\subsection{Cấu hình file mô phỏng SUMO}

Các file \texttt{.sumocfg}, \texttt{.net.xml}, \texttt{.rou.xml}, \texttt{.add.xml} được thiết lập như sau:
\begin{itemize}
    \item \texttt{net.xml}: Định nghĩa topology mạng lưới, làn, nút giao, kết nối.
    \item \texttt{rou.xml}: Định nghĩa luồng xe, loại phương tiện, tần suất xuất hiện.
    \item \texttt{add.xml}: Logic đèn tín hiệu, chuỗi pha, trạng thái, thời lượng.
    \item \texttt{sumocfg}: Tổng hợp các file trên, tham số thời gian mô phỏng.
\end{itemize}

\subsection{Cấu hình tích hợp bộ điều khiển}

\begin{itemize}
    \item Khởi tạo UniversalSmartTrafficController với danh sách nút giao, làn, logic đèn.
    \item Cấu hình các tham số min\_green, max\_green, alpha, threshold tắc nghẽn.
    \item Giao tiếp TraCI: subscription các biến trạng thái (queue, speed, waiting), điều khiển pha.
    \item Ghi log Supabase: đồng bộ sự kiện, reward, trạng thái APC.
\end{itemize}

\section{Thí nghiệm kiểm soát}

Các thí nghiệm kiểm soát nhằm đánh giá hiệu năng hệ thống trong các tình huống đặc biệt và kịch bản thực tế:

\subsection{Tình huống giả lập}

\begin{itemize}
    \item \textbf{Xuất hiện xe ưu tiên và chuyển trạng thái giao thông}: Xe ưu tiên được đưa vào mạng lưới từ đầu. Sau đó, hệ thống gia tăng lưu lượng, chuyển từ trạng thái thông thoáng sang tắc nghẽn để kiểm tra khả năng phản ứng của bộ điều khiển.
    \item \textbf{Kiểm thử adaptive RL}: Đánh giá thời gian chờ, độ dài hàng chờ, tần suất gridlock, phục hồi sau tắc nghẽn.
    \item \textbf{Kịch bản xuất hiện xe ưu tiên}: Đo thời gian phục vụ xe ưu tiên, kiểm tra hiệu quả green wave, đánh giá ảnh hưởng tới các hướng khác.
    \item \textbf{Kịch bản tắc nghẽn cục bộ}: Kích hoạt congestion mode, đo thời gian giải tỏa, đánh giá khả năng phối hợp corridor giữa các nút.
    \item \textbf{Kịch bản rẽ trái bị chặn}: Phát hiện trường hợp rẽ trái bị cản trở, tạo pha rẽ trái bảo vệ động, đo hiệu quả giải tỏa dòng xe.
    \item \textbf{Kịch bản starvation}: Đánh giá khả năng phục hồi phục vụ các hướng bị bỏ qua hoặc ưu tiên thấp.
\end{itemize}

\subsection{Quy trình thực nghiệm}

\begin{enumerate}
    \item Chạy mô phỏng với cấu hình traffic demand, logic đèn, tham số mặc định.
    \item Theo dõi trạng thái qua SUMO GUI, dashboard, log sự kiện lên Supabase.
    \item Thu thập dữ liệu về thời gian chờ, độ dài hàng chờ, số lần gridlock, số lần phục vụ xe ưu tiên, hiệu quả protected left.
    \item Phân tích kết quả, so sánh với baseline, đánh giá theo từng kịch bản.
\end{enumerate}

\subsection{Tích hợp dashboard trực quan hóa}

Dữ liệu sự kiện, trạng thái và hiệu năng được trực quan hóa qua dashboard (SmartIntersectionTrafficDisplay), giúp đánh giá kết quả mô phỏng theo thời gian thực.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{adadf.png}
    \caption{Dashboard trực quan hóa kết quả thí nghiệm mô phỏng SUMO}
    \label{fig:sumo_dashboard}
\end{figure}